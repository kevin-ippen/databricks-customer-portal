[build]
  command = "npm install && npx vite build"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "ALLOWALL"
    Content-Security-Policy = "frame-ancestors *;"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
